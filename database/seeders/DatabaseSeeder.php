<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    public function run(): void
    {
        // Intentionally empty.
        // Create real accounts through the SignUp page so users_auth stays in sync.
    }
}
